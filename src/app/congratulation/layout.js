import React from 'react'

export const metadata = {
    title: 'Congratulation',
    description: 'Congratulation desc'
}
export default function LayoutCong({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}
